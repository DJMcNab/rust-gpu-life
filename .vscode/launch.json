{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/target/release/rust-gpu-life.exe",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
